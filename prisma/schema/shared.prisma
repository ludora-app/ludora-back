// shared.prisma

enum InvitationStatus {
  PENDING
  ACCEPTED
  REJECTED
  CANCELED

  @@map("InvitationStatus")
  @@schema("shared")
}

enum Currency {
  EUR

  @@map("Currency")
  @@schema("shared")
}
